# План работ — Кусь (на 2026-06-19)

Хэндофф для продолжения работы. Сегодня сделан переход на модель «отчёт проверяет
кинолог перед отправкой» + позиционирование «эксперт, а не ИИ». Код задеплоен на прод
5.42 и работает. Ниже — что осталось.

## Откуда работать / доступы

- Прод: **5.42** (`/opt/kus`), сервисы `kus.service` (uvicorn :8007) и `kus-bot.service`.
- Эта рабочая копия `/mnt/5.42-opt/kus` = sshfs-монтаж `/opt/kus` (правки = сразу на сервере).
- С Амстердама есть ssh: `ssh root@5.42.101.215` (тот же ключ, что у ночных кронов).
- **Деплой** (рестарт после правок .py; шаблоны/статика подхватываются без рестарта):
  ```
  ssh root@5.42.101.215 'cd /opt/kus && bash tools/deploy.sh'      # гейт → рестарт kus.service
  ssh root@5.42.101.215 'systemctl restart kus-bot.service'         # бот отдельно
  ```
- Админки (Basic-auth, логин/пароль в `/opt/kus/.env`):
  `/admin` · **`/admin/review`** (очередь кинолога) · `/admin/support` · `/admin/evals` · `/admin/qa`
- Ворота перед коммитом фикса согласования: `bash tools/check.sh` (exit≠0 = регресс).

## СДЕЛАНО (задеплоено, работает в проде)

- Формулировки: убран «ИИ/нейросеть» из клиентских текстов → «по ветнормам, проверяет
  специалист»; в оферте/политике автоматизация раскрыта мягко. Сроки везде «до 3 раб. дней».
- Поток ревью: `paid → processing → review → done` (+ `rework`). Авто-проверка (агент) =
  инварианты `reconcile.py`, затем кинолог в `/admin/review`: одобрить / правка текстов /
  на переделку с замечаниями. Клиенту НИЧЕГО не уходит автоматически.
- Комбо-заказы: отдельные sidecar (natural/dry), две формы правки.
- Доставка по каналам: email / Telegram (deep-link) / ВКонтакте (`VK_TOKEN`); выбор канала в анкете.
- Инвариант «суточная норма = сумма групп» в `reconcile.py` + инъекция в `canary.py` (5/5).
- Скриншоты примера на лендинге перегенерены, слайдер переразмечен на чистые страницы.
- Вебхук YooKassa перепроверяет платёж в API (`app._verify_yookassa_payment`).

## Статус Codex на 2026-06-27

Сделано без владельца:
- [x] Добавлен корневой `AGENTS.md` для Codex: архитектура, прод-топология,
      правила безопасности, проверки, текущие риски и план.
- [x] Синхронизирован устаревший блок в `CLAUDE.md`: инвариант
      «суточная норма = сумма групп» уже есть в `reconcile.py`.
- [x] `app.submit_order` теперь аккуратно возвращает 422 при кривом возрасте,
      весе или Telegram ID, а не падает raw `ValueError`.
- [x] `app.yookassa_webhook` теперь игнорирует malformed JSON, нечисловой
      `order_id` и неизвестный заказ без шумного 500.
- [x] На странице статуса и в email-доставке добавлена VK first-contact логика:
      клиенту объясняется, что перед доставкой в ВК нужно первым написать
      сообществу. Добавлен `VK_COMMUNITY_URL` в `.env.example`.

Проверка:
- [x] `bash tools/check.sh` на 5.42 прошёл: канарейка жива, golden-снапшот совпал,
      согласование 17/17 профилей чистое.
- [ ] В текущей Codex-среде локальный `tools/check.sh` всё ещё блокируется
      отсутствующей системной библиотекой WeasyPrint `libpangoft2-1.0-0`; для
      локальной проверки нужен Pango.

## СДЕЛАТЬ СЕГОДНЯ

### 1. Конфиг в `/opt/kus/.env` (без этого половина не работает) — ПРИОРИТЕТ
- [ ] `SMTP_HOST/PORT/USER/PASSWORD/FROM` — сейчас ПУСТО → email-доставка и письма
      «заказ на проверку» НЕ уходят. Это основной канал. (egress SMTP с 5.42 работает.)
- [ ] `ADMIN_EMAIL` — куда слать уведомления о ревью (по умолчанию = SMTP_USER).
- [ ] `VK_TOKEN` — токен сообщества ВК (права messages), если нужна авто-доставка в ВК.
- [ ] Сменить `ADMIN_PASS` (и желательно `ADMIN_USER`) — `/admin*` на публичном URL.
- [ ] После правок: `ssh root@5.42.101.215 'systemctl restart kus.service kus-bot.service'`

### 2. ВКонтакте/Telegram «написать первым»
- [x] Добавить на страницу статуса (`templates/status.html`) подсказку/кнопку «Написать нам
      ВКонтакте» — кнопка появится, когда задан `VK_COMMUNITY_URL`.
- [x] То же в email-письме: подсказать, что для получения в ВК надо первым написать сообществу.
- [ ] Добавить аналогичную подсказку в бот, если владелец хочет VK как отдельный сценарий
      поддержки из Telegram.
- [ ] Проверить, что VK-доставка реально проходит на тестовом пользователе, который написал сообществу.

### 3. Проверка потока end-to-end
- [ ] Тестовый заказ (при пустой YooKassa — тест-режим) → дойдёт до `/admin/review`.
- [ ] Прогнать: «правка текстов» (re-render без Gemini), «на переделку с замечаниями»,
      «одобрить» → проверить доставку в выбранные каналы.
- [ ] Проверить комбо-заказ (две формы правки).

### 4. Хвосты качества (по желанию, не блокеры)
- [ ] Подумать, не убрать ли пустую «хвостовую» страницу меню (12 лог. → 14 физ. страниц
      при weasyprint; меню разливается на 4 страницы — page_6 полупустая).
- [ ] Уведомление кинолога: сейчас email + TG(если доступен). Если кинолог не на 5.42 —
      убедиться, что email доходит (зависит от п.1 SMTP).

## Полезное

- Жизненный цикл, страницы отчёта, дыры — описаны в `CLAUDE.md` (разделы про ревью/доставку).
- Память агента: `/root/.claude/projects/-mnt-5-42-opt-kus/memory/` (deploy-доступ, egress, и др.).
- Не коммить вручную с гейтом — авто-backup-крон коммитит всё; гейт только на деплое.
